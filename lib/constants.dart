import 'package:flutter/material.dart';

const menuTitles = [
  'Profile',
  'Theme',
  'Logout',
];

const initialDelayTime = Duration(milliseconds: 150);
const itemSlideTime = Duration(milliseconds: 300);
const staggerTime = Duration(milliseconds: 150);
const buttonDelayTime = Duration(milliseconds: 180);
const buttonTime = Duration(milliseconds: 700);
